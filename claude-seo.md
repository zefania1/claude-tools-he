# Claude SEO · מדריך ההתקנה בעברית

> נכתב עבור מי שביקש את המדריך בתגובות לריל. כל מספר כאן נמדד ב-**4.9.2026**
> מול ה-API של גיטהאב ומול ה-README של הפרויקט, ולא הועתק מהזיכרון.

**מה זה:** פלאגין SEO חינמי לקלוד קוד. ‏25 תת-סקילים ו-18 סוכנים רצים במקביל וסורקים
אתר: ‏SEO טכני, איכות תוכן, ‏Schema, נראוּת במנועי-AI, ‏SEO מקומי. הפלט הוא **תוכנית
פעולה מתועדפת**, לא ציון בודד.

| | |
|---|---|
| כוכבים | **16,287** · ‏2,385 forks [GitHub API, 4.9.26] |
| רישיון | **MIT**, חופשי כולל מסחרי |
| נדחף לאחרונה | 26.8.2026 · פרויקט חי |
| הריפו | https://github.com/AgriciDaniel/claude-seo |

---

## 🔴 תיקון לפקודה שבתגובה

הפקודה שפרסמתי בתגובה, `/plugin install claude-seo`, חלקית ולא תעבוד לבדה.
צריך קודם להוסיף את המרקטפלייס, ושם-הפלאגין המלא כולל סיומת. הנכונות למטה.

---

## מה צריך לפני

- **קלוד קוד** מותקן (גרסה 1.0.33 ומעלה) · https://claude.com/claude-code
- **Python 3.10 ומעלה**. ראו הסעיף על 3.9 למטה, זה מכשול אמיתי
- טרמינל. זה פלאגין למפתחים ולמי שמוכן לעבוד בשורת פקודה, **לא אפליקציה**

## ההתקנה · שלוש שורות בתוך קלוד קוד

פותחים טרמינל בתיקייה כלשהי, מריצים `claude`, ואז בתוך קלוד קוד:

```
/plugin marketplace add AgriciDaniel/claude-seo
```
```
/plugin install claude-seo@agricidaniel-claude-seo
```
```
/seo setup
```

`‎/seo setup` בונה סביבת פייתון **מבודדת** בתוך התיקייה של קלוד קוד ומתקין דפדפן
‏Chromium לצרכי סריקה. הוא **לא** מתקין חבילות גלובליות ולא נוגע ב-PATH שלכם.

**בדיקה שהכל תקין:**
```
/seo doctor
```

## ההרצה הראשונה

```
/seo audit https://האתר-שלכם.co.il
```

פקודות נוספות שימושיות: ‏`/seo page <url>` (עמוד בודד לעומק) · ‏`/seo schema <url>`
(סימון מובנה) · ‏`/seo geo <url>` (איך מנועי-AI קוראים אתכם) · ‏`/seo sitemap generate`.

---

## 🔴 מה לא עובד, ומה עושים

**‏`python3` שלכם הוא 3.9 והכלי דורש 3.10+.**
זה קרה לי על המק. ‏macOS מגיע עם 3.9.6, וההתקנה נעצרת עם הודעה ברורה
(*"requires Python 3.10 or newer"*), והיא **לא** נופלת בשקט.
הפתרון: מתקינים פייתון עדכני (`brew install python@3.11`) ומריצים `/seo setup` שוב.

**ההתקנה הידנית, אם הפלאגין לא נתפס:**
```
git clone --depth 1 https://github.com/AgriciDaniel/claude-seo.git
```
```
bash claude-seo/install.sh
```

**הסרה מלאה:**
```
bash claude-seo/uninstall.sh
```

---

## מה חינם ומה לא, בלי טשטוש

**החינמי, וזה מה שהראיתי בריל:** הכלי עצמו MIT, וה-free tier **אינו מבצע שום קריאה
לצד שלישי בתשלום.** הוא מושך את העמודים שלכם, מנתח אותם מקומית ומחזיר תוכנית פעולה.

**מה שדורש תוספת:**
- **מפתחות Google API** (חינמיים להשגה, ‏Tier 0). בלעדיהם נתוני מהירות-האתר הם
  **הערכת-מעבדה** ולא נתוני-שטח אמיתיים, ומצב האינדוקס **מוסק** ולא נקרא מגוגל.
- **הרחבות MCP** (‏Ahrefs · DataForSEO · SE Ranking) לנתוני מתחרים ומילות-מפתח.
  אלה שירותים בתשלום עם חשבון משלהם.

**ומה שחשוב לומר:** הכלי **מאבחן ובונה תוכנית תיקון**. הוא לא מתקן את האתר בשבילכם.

---

## דוגמה אמיתית · מה הוא מצא אצלי

הרצתי אותו על אתר נחיתה משלי ב-31.8.2026. ארבעה ממצאים בדקות:

| מה נבדק | התוצאה |
|---|---|
| ‏Schema (סימון מובנה) | **0 בלוקים**, לגוגל ולמנועי-AI אין מה לצטט כ"מי זה" |
| ‏Canonical | **חסר** |
| ‏Sitemap | **לא נמצא** |
| קישורים פנימיים | **0** (מול 24 חיצוניים) |
| ‏Agent-UX (כמה האתר קריא לסוכני-AI) | **100/100** |
| איכות תוכן | **92/100** · אפס טקסט-מילוי |

התמונה במשפט: *האתר קריא ל-AI בצורה מצוינת, אבל אין בו שום סימון שאומר לו מי אני.*
זו בדיוק הזווית שכלי כזה שווה עליה את הזמן.

---

**נתקעתם?** כתבו לי בתגובות או בהודעה פרטית ואני אעזור. בר צפניה
