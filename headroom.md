# Headroom · מדריך התקנה

> נכתב 20.8.26. כל פקודה כאן **הורצה אצלנו** לפני שנכתבה.
> המקור: `github.com/headroomlabs-ai/headroom` · Apache-2.0 · 66,955★ [נמדד 20.8].

## מה זה עושה
פרוקסי **מקומי** שיושב בין קלוד קוד למודל. כל פלט-כלי שנשלח (לוג, JSON,
קריאת-קובץ, תוצאת-שאילתה) עובר דרכו ומצטמצם לפני שהוא נספר כטוקנים.
הנתונים לא עוזבים את המחשב.

## התקנה

```
uv tool install --python 3.13 "headroom-ai[all]"
```

ואז, במקום להריץ `claude` רגיל:

```
headroom wrap claude
```

**אמור לחזור:** שורת אישור שהפרוקסי עלה על פורט 8787, ואחריה קלוד קוד רגיל.

חלופה בלי `uv`:
```
pip install "headroom-ai[all]"
```

## דרישות
- Python 3.10 ומעלה (‏3.13 לחישובי-עלות בדשבורד)
- מפתח של ספק המודל שאתם כבר משתמשים בו. הוא לא מחליף אותו

## 🔴 מה שכדאי לדעת לפני

| הציפייה | מה שנמדד |
|---|---|
| "מכווץ לי את הקוד" | **לא.** קובץ קוד קיבל 0.0% אצלנו (`router:noop`) |
| "60–95% חיסכון" | זה **מטען JSON**. התיעוד אומר 15–20% על סשן-קידוד |
| מה שנמדד אצלנו | **32.4%** על חמישה קבצים אמיתיים · 48.8% על הכבד ביותר |

⚠️ המדידה שלנו רצה על **מסלול-הכללים בלבד**. מודל ה-ML לא אושר כטעון,
ולכן 32.4% הוא **חסם תחתון**, לא תקרה.

## איך לבדוק אצלכם
הריצו סשן רגיל דרך `headroom wrap claude`, ואז `/cost` בתוך קלוד קוד.
השוו למספר שאתם מכירים מסשן דומה בלי הפרוקסי.
