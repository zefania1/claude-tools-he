# חמשת הפלאגינים לקלוד קוד · מדריך ההתקנה בעברית

> נכתב עבור מי שכתב "פלאגין" בתגובות לריל. כל פקודה כאן נקראה ישירות מקובץ
> ההגדרות של הפרויקט (`.claude-plugin/marketplace.json`) ב-**13.9.2026**,
> ולא הועתקה מהזיכרון. המספרים נמדדו מול ה-API של גיטהאב ב-**10.9.2026**.

**מה זה:** פלאגין בקלוד קוד הוא חבילה שמשנה איך קלוד עובד, בלי שתשנו שום דבר
בפרויקט שלכם. חמישה כאלה עושים את ההבדל הגדול ביותר, ואחד מהם כבר מותקן אצלכם
ורוב האנשים לא פתחו אותו.

| | |
|---|---|
| כמה זה עולה | **חינם, כל החמישה.** קוד פתוח, MIT או Apache 2.0 |
| מה כן עולה | השימוש בקלוד עצמו, בדיוק כמו קודם |
| כמה פקודות | **3 עד 5**, לא אחת. הסיבה למטה |
| מה צריך | קלוד קוד מותקן. זה הכול |

---

## הדרך הכי מהירה: שניים בפקודה אחת כל אחד

קלוד קוד מוסיף את המרקטפלייס הרשמי של Anthropic **אוטומטית בהפעלה
האינטראקטיבית הראשונה**. אם הרצתם קלוד קוד פעם אחת, הוא כבר אצלכם, ושניים
מהחמישה יושבים בפנים.

פותחים קלוד קוד ומדביקים:

```
/plugin install superpowers@claude-plugins-official
```

```
/plugin install code-review@claude-plugins-official
```

ואז, כדי לראות מה מותקן:

```
/plugin
```

## השניים האחרים: מרקטפלייס חיצוני, שתי פקודות כל אחד

**Ponytail** — מכריח את קלוד לכתוב את הפתרון הקצר ביותר שעובד:

```
/plugin marketplace add DietrichGebert/ponytail
```

```
/plugin install ponytail@ponytail
```

**claude-mem** — נותן לקלוד זיכרון בין סשנים:

```
/plugin marketplace add thedotmack/claude-mem
```

```
/plugin install claude-mem@thedotmack
```

⚠️ **שימו לב לשורה האחרונה.** שם המרקטפלייס של claude-mem הוא **thedotmack**,
לא claude-mem. אם תכתבו `claude-mem@claude-mem` זה ייכשל. זה המקום היחיד
במדריך שבו טעות הקלדה סבירה מפילה את ההתקנה.

## והחמישי כבר אצלכם

**המרקטפלייס הרשמי של Anthropic** אינו פלאגין שמתקינים, אלא המדף שמכיל אותם.
אין מה להריץ, רק לפתוח:

```
/plugin
```

בפנים, על המחשב שלנו, נמדדו **291 רשומות** ו-**39 תיקיות פלאגין רשמיות**
(נמדד 12.9.2026). שם יושבים גם הסקילים השימושיים: יצירת סקילים, פיתוח שרתי
MCP, עיצוב פרונטאנד, פישוט קוד, ועוד.

---

## 🔴 מה לא נכנס לסרטון, וכדאי לדעת לפני

**אין פקודה אחת שמתקינה את כולם.** שני מרקטפלייסים נפרדים, ולכן לפחות שלוש
פקודות. כל מי שמבטיח לכם שורה אחת לחמישה, לא פתח את קובצי ההגדרות.

**Ponytail לא חוסך לכם חצי מהשימוש.** מסתובבת ברשת טענה על "מעל חמישים אחוז".
המדידה הבלתי תלויה היחידה שמצאתי (JetBrains, יולי 2026, 80 משימות מזווגות)
הראתה **חציון 15 אחוז פחות קוד ו-10.3 אחוז פחות עלות**. זה עדיין הרבה. זה לא
חצי. הכלי עושה דבר אחד ועושה אותו טוב: YAGNI, ספריית תקן לפני ספרייה חיצונית,
בלי הפשטות שלא ביקשתם.

**code-review אינו פרויקט נפרד.** הוא יושב בתוך המרקטפלייס הרשמי
(`./plugins/code-review`), בבעלות Anthropic. כל ספירת כוכבים שתראו מיוחסת לו
בנפרד היא שגויה. מה שהוא כן עושה: חמישה סוקרים בלתי תלויים רצים במקביל
(תאימות ל-CLAUDE.md, איתור באגים, הקשר מהיסטוריית git, הערות PR קודמות, אימות
הערות קוד), וסף ביטחון 80 מסנן את מה שמגיע אליכם.

**claude-mem — מה שלא בדקתי.** הרישיון Apache 2.0 והפלאגין עצמו חינם. את
דרישות הריצה שלו (תלויות, תהליכי רקע) **לא בדקתי על מכונה נקייה**, ולכן אני
לא מבטיח "אפס הגדרה". אם אתם רגישים לזה, קראו את ה-README בריפו לפני ההתקנה.

**superpowers הוא הכבד מכולם.** הוא לא מוסיף פקודה, הוא מוסיף שיטות עבודה:
לחשוב לפני שכותבים, לתקן לפי ראיות, לבדוק את עצמו לפני שמכריזים על סיום.
אנחנו מריצים אותו כאן כל יום. אם אתם מתקינים רק אחד מהחמישה, זה הוא.

---

## המספרים, עם תאריך קריאה

| הפרויקט | כוכבים | רישיון | עדכון אחרון |
|---|---|---|---|
| obra/superpowers | **284,184** · 25,422 forks | MIT | 10.9.2026 |
| DietrichGebert/ponytail | **133,783** · 7,155 forks | MIT | 7.9.2026 |
| thedotmack/claude-mem | **93,590** · 8,229 forks | Apache-2.0 | 10.9.2026 |
| anthropics/claude-plugins-official | **36,091** · 4,047 forks | Apache-2.0 | 9.9.2026 |

כולם מ-GitHub API, 10.9.2026. מספר שרק עולה נכתב כאן כרצפה, לא כתקרה.

נתקעתם? כתבו לי בהודעה פרטית באינסטגרם [@barzefania](https://instagram.com/barzefania).
